# ============================================================
# Nexus monolito — backend (psycopg3) + Oficina (NiceGUI/asyncpg)
# Uniao dos requirements dos dois projetos. Runtime only.
# ============================================================

# --- Web / ASGI (compartilhado) ---
fastapi[standard]>=0.115.0
uvicorn[standard]>=0.30.0
orjson>=3.10.0
starlette-compress>=1.7.0
asgi-correlation-id>=4.3.0
sse-starlette>=2.0.0

# --- Backend: banco psycopg3 + config + utilidades ---
psycopg[binary,pool]>=3.2.0
pydantic-settings>=2.0.0
d20>=1.1.2
transitions>=0.9.0
slowapi>=0.1.9
sentry-sdk[fastapi]>=2.0.0

# --- Oficina: banco SQLModel/asyncpg + auth + migracoes ---
sqlmodel>=0.0.16
sqlalchemy[asyncio]>=2.0.25
asyncpg>=0.29.0
alembic>=1.13.0
bcrypt>=4.0
python-dotenv>=1.0.0

# --- Oficina: UI ---
nicegui>=3.10.0,<4.0.0

# --- Oficina: armazenamento R2 (boto3, usado por r2_storage.py) ---
boto3>=1.34.0

# --- Oficina: geracao de imagem (atelie / pipeline) ---
fal-client>=0.5.0
google-genai[aiohttp]>=1.60.0
openai>=1.60.0
tenacity>=9.0.0
Jinja2>=3.1.4
Pillow>=10.4.0

# --- Narrador / LLM (Anthropic Claude) ---
anthropic>=0.40.0

# --- Logging estruturado (compartilhado; versao mais alta dos dois) ---
structlog>=24.4.0

# --- HTTP client (fal-client + testes do backend) ---
httpx>=0.27.0

# ============================================================
# Dev/test (NAO instalados em producao). Para rodar os testes:
#   pip install "pytest>=8.0.0" "pytest-asyncio>=0.23.0" "hypothesis>=6.100.0"
# ============================================================
